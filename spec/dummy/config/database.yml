common: &common
  adapter: mysql2
  encoding: utf8
  reconnect: false
  pool: 5
  username: root
  password:
  host: localhost

development:
  <<: *common
  database: "super_role_dev"

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *common
  database: "super_role_test"

staginglocal:
  <<: *common
  database: "super_role_staglocal"

staging:
  <<: *common
  database: "super_role_stag"

production:
  <<: *common
  database: "super_role_prod"